# oiw-groovy-fwd

Groovy parity case: sender -> script.groovy (SAP Message API dialect, JVM
bridge) -> RR-forward -> PD terminator + companion listener. Script shape
mirrored from order-to-s4's normalizeOrder (proven Script activityType).
