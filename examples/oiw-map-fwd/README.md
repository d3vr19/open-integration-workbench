# oiw-map-fwd

XSLT-mapping parity case (B-3): RR-warmup -> transform.xslt (Saxon-HE XSLT 2.0
via the JVM bridge locally; XSLTMapping exporter shape on the tenant) ->
RR-forward -> PD terminator + companion listener.

Mapping shape mirrored verbatim from tenant ground truth (running flow) +
the SAP Hub TPM V2 reference (packages/pattern-book/shapes/Mapping-XSLTMapping.yaml).
